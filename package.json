{
  "name": "mindbloging",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd backend && npm install",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install --legacy-peer-deps",
    "start": "cd backend && npm start",
    "deploy": "npm run install-all && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["blog", "mern", "react", "nodejs", "mongodb"],
  "author": "",
  "license": "ISC",
  "description": "MindBloging - A full-stack blog platform built with MERN stack",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
